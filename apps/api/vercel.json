{
  "version": 2,
  "installCommand": "NODE_ENV=development pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm turbo build --filter=@repo/database && pnpm turbo build --filter=api",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/main.js"
    }
  ]
}